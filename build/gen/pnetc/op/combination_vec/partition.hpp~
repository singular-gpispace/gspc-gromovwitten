#ifndef _PNETC_OP_combination_vec_partition
#define _PNETC_OP_combination_vec_partition

#include <list>
#include <we/type/value.hpp>

namespace pnetc
{
  namespace op
  {
    namespace combination_vec
    {
      void partition
        ( const std::list<pnet::type::value::value_type>& v
        , std::list<pnet::type::value::value_type>& vec
        , int& nb
        );
    }
  }
}
#endif
